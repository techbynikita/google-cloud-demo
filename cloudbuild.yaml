steps:
  # Build and push backend image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'asia-south1-docker.pkg.dev/techbynikita/feedback-wall/feedback-wall-backend:latest'
      - './backend'
    id: 'build-backend'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'asia-south1-docker.pkg.dev/techbynikita/feedback-wall/feedback-wall-backend:latest'
    id: 'push-backend'

  # Deploy backend to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'feedback-wall-backend'
      - '--image'
      - 'asia-south1-docker.pkg.dev/techbynikita/feedback-wall/feedback-wall-backend:latest'
      - '--region'
      - 'asia-south1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '8080'
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '10'
    id: 'deploy-backend'

  # Get backend URL and build frontend with it
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: bash
    args:
      - '-c'
      - |
        BACKEND_URL=$$(gcloud run services describe feedback-wall-backend --region=asia-south1 --project=techbynikita --format='value(status.url)')
        echo "Backend URL: $$BACKEND_URL"
        docker build -t asia-south1-docker.pkg.dev/techbynikita/feedback-wall/feedback-wall-frontend:latest --build-arg VITE_API_URL=$$BACKEND_URL ./frontend
        docker push asia-south1-docker.pkg.dev/techbynikita/feedback-wall/feedback-wall-frontend:latest
    id: 'build-and-push-frontend'
    waitFor: ['deploy-backend']

  # Deploy frontend to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'feedback-wall-frontend'
      - '--image'
      - 'asia-south1-docker.pkg.dev/techbynikita/feedback-wall/feedback-wall-frontend:latest'
      - '--region'
      - 'asia-south1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '8080'
      - '--memory'
      - '256Mi'
      - '--cpu'
      - '1'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '10'
    id: 'deploy-frontend'
    waitFor: ['build-and-push-frontend']

options:
  logging: CLOUD_LOGGING_ONLY
