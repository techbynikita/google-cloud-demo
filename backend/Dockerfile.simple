FROM node:18-alpine

WORKDIR /app

# Copy package.json
COPY package.json ./

# Install only express and cors (minimal dependencies)
RUN npm install express cors --save

# Copy simple server
COPY server-simple.js server.js

# Expose port
EXPOSE 8080

# Start the server
CMD ["node", "server.js"]

